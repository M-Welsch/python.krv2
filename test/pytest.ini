[pytest]
testpaths =
    unit
    integration
    hardware
markers =
    onraspi: only working on raspi
    performance: perfomance test that takes long. To be skipped by default and only called on current demand